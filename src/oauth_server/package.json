{
  "name": "oauth_server",
  "version": "1.0.0",
  "description": "",
  "main": "passport.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {},
  "dependencies": {},
  "comments": {
    "root_dependencies": {
      "cors": "^2.8.5",
      "express": "^4.17.1",
      "passport": "^0.4.0",
      "passport-facebook-token": "^3.3.0",
      "passport-google-oauth20": "^2.0.0",
      "passport-google-plus-token": "^2.1.0",
      "passport-google-token": "^0.1.2",
      "passport-jwt": "^4.0.0",
      "passport-local": "^1.0.0",
      "bcryptjs": "^2.4.3"
    },
    "moved-to-root":{
      "dependencies": {
        "express-promise-router": "^3.0.3",
        "joi": "^14.3.1",
        "jsonwebtoken": "^8.5.1",
        "morgan": "^1.9.1"
      }
    },
    "passport_notes": [
      "install passport and passport strategies in the root",
      "require passport and passport config from the root server",
      "to use jwt authentication require passport and jwt strategy in router then add as middleware",
      "GOTCHA: if bcrypt fails to install, delete it from the dependencies and install it separately"
    ]
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
